#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prints the alphabet in lowercase, and then in uppercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	char chr;

	for (chr = 'a'; chr <= 'z'; chr++)
		putchar(chr);
		printf('\n')
	for (chr = 'A'; chr <= 'Z'; chr++)
		putchar(chr);



	return (0);
}
